# [OPEN] large-graph-lag

## 背景

- 问题：当节点和连线数量增多后，画布交互出现明显卡顿。
- 目标：在不改变任何现有功能和交互语义的前提下，优化实现与结构，降低大规模数据下的卡顿。

## 当前假设

1. 主画布每次状态变化都会全量重绘所有节点和连线，导致帧时间随对象数量线性上升。
2. 鼠标移动、拖拽、缩放过程中存在高频 `setState` 与命中计算，触发 React 和 SVG/Canvas 双重重算。
3. 焦点导航、自动聚焦、搜索候选或包围盒计算在每次渲染都重新遍历全部对象，造成额外开销。
4. 节点与连线的几何计算、标签尺寸计算和路径控制点计算缺少缓存，重复成本过高。
5. 部分 UI 面板或派生数据 `useMemo` / `useCallback` 依赖过宽，导致无关状态变化也触发重算与重渲染。

## 调试计划

1. 只添加性能埋点，不改业务逻辑。
2. 运行项目并在大图场景下复现卡顿。
3. 读取埋点结果，确认热点路径。
4. 基于证据做最小范围优化。
5. 对比优化前后结果并回归验证。

## 证据

- 样本图规模：252 个节点，514 条连线，总对象数 766。
- `pre-fix` 日志显示：
  - 平均渲染耗时约 `548ms ~ 667ms`
  - `measureTextCount` 单批约 `23666 ~ 47332`
  - `adaptiveCount` 单批约 `4616 ~ 9232`
- `post-fix` 日志显示：
  - 平均渲染耗时约 `43.9ms ~ 44.1ms`
  - `measureTextCount` 降到 `2252`
  - `adaptiveCount` 降到 `3958`

## 结论

- 假设 4 成立：文本测量与自适应尺寸计算缺少缓存，是主要瓶颈。
- 假设 1 部分成立：当前仍然是全量渲染，但在文本尺寸热点被削减后，整体卡顿已大幅下降。
- 假设 3 部分成立：`find`/`includes` 这类按次遍历也有放大效应，因此补充了 `Map` / `Set` 索引。
- 假设 2、5 目前不是主要矛盾，至少不是这轮样本中的第一瓶颈。

## 已实施优化

1. 文本测量改为复用单个测量 canvas，并缓存 `font + text` 结果。
2. 自适应文本盒尺寸改为按配置和文本内容缓存。
3. `nodes` / `connections` 查询改为 `Map` 索引。
4. 选中判断改为 `Set`，避免高频 `includes`。
5. 连线标签尺寸仅在真正需要显示标签或进入编辑时才计算。

## 当前状态

- 状态：待用户验证
- 调试埋点与调试服务仍保留，等待确认后再清理

## 补充现象排查

- 用户反馈“加载数据后闪现一下又消失”。
- 追加埋点后，`applyImportedData` 已确认成功接收 `252` 个节点和 `514` 条连线。
- 但没有采到任何 `setNodes([])` 路径触发的“graph cleared after non-empty state”日志。
- 浏览器网络记录显示：
  - `3000` 端口页面在导入后发生了新的 `Document` 请求，属于整页重载。
  - 切换到静态构建页 `3001` 后，相同导入动作不会消失，图数据稳定保留。

## 补充结论

- “闪现后消失”不是本轮缓存优化把图清空了。
- 当前证据表明，这是开发态页面重载导致的内存状态丢失。
- 在静态构建环境中，本轮性能优化与导入功能可同时正常工作。
